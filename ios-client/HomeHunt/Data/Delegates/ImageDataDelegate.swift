import Foundation

protocol ImageDataDelegate {
    func didReceiveImage(image: Data, error: Any?)
}

extension AddDataDelegate {
    func didReceiveImage(image: Data, error: Any?) {
        
    }
}
